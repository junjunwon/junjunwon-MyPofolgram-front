// import axios from 'axios'
import http from '../../utils/http'

export default {
    // async getCommentSize({commit, param}) {
    //     const response = await http.get('/api/post/getCommentSize', param)
    //     commit('setCommentSize', response.data.result)
    // }
}